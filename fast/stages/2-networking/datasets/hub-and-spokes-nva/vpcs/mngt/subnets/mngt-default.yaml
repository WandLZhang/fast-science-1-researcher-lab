# skip boilerplate check

# yaml-language-server: $schema=../../../../../schemas/subnet.schema.json

name: mngt-default
region: $locations:primary
ip_cidr_range: 10.75.0.0/24
description: Default primary-region subnet for Palo Alto management VPC
