# skip boilerplate check
---
# start of document (---) avoids errors if the file only contains comments

# yaml-language-server: $schema=../../../../schemas/vpc.schema.json

# Palo Alto NGFW untrust (internet-facing) VPC. Hosts the regional Cloud
# NAT used by the N/S firewall MIG for outbound traffic that the firewalls
# decide to forward to the internet. NOT attached to the NCC hub.

project_id: $project_ids:net-prod-0
name: untrust-0
delete_default_routes_on_create: true
mtu: 1500
nat_config:
  nat-primary:
    region: $locations:primary
routes:
  default:
    dest_range: 0.0.0.0/0
    next_hop_type: "gateway"
    next_hop: "default-internet-gateway"
