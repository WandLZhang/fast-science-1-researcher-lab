
Great direction. Here's the full plan covering all three things:

---

## Part 1: Clone the repos locally

Straightforward — clone both into the Projects folder:
- `workload-viz-nextflow-batch/`
- `workload-viz-medsiglip-pathology/`

---

## Part 2: Create `fast-science-1-researcher-lab` (L1)

This extracts the **common IT admin pattern** from all three workload-viz backends. Looking at the toxicology `main.py`, the IT infra steps are:

| Step | Function | What it does |
|------|----------|-------------|
| `enable-apis` | `execute_enable_apis()` | Enables 6 GCP APIs via Service Usage API |
| `create-sa` | `execute_create_service_account()` | Creates project-level SA |
| `iam-roles` | `execute_iam_roles()` | Grants 5 IAM roles to SA |
| `org-policies` | `execute_configure_org_policies()` | Overrides org policies (e.g., shieldedVm for GPU) |
| `create-network` | `execute_create_network()` | VPC + firewall + Private Google Access |
| `configure-cloud-nat` | `execute_configure_cloud_nat()` | Cloud Router + NAT config |

These are identical across nextflow, medsiglip, and toxicology — only the **API list**, **IAM roles**, and **org policy overrides** differ per researcher/workload.

### Proposed L1 Structure:

```
fast-science-1-researcher-lab/
├── README.md                           # "How IT admins provision researcher projects"
│
├── CommonComponents/                   # Reusable Terraform modules
│   ├── ApiAllowlistModule/
│   │   ├── main.tf                     # google_project_service resources
│   │   ├── variables.tf                # var.project_id, var.apis (list)
│   │   └── outputs.tf
│   ├── ServiceAccountModule/
│   │   ├── main.tf                     # google_service_account + display_name
│   │   ├── variables.tf                # var.project_id, var.sa_name, var.display_name
│   │   └── outputs.tf
│   ├── IAMRolesModule/
│   │   ├── main.tf                     # google_project_iam_member for each role
│   │   ├── variables.tf                # var.project_id, var.sa_email, var.roles (list)
│   │   └── outputs.tf
│   ├── OrgPoliciesModule/
│   │   ├── main.tf                     # google_org_policy_policy overrides
│   │   ├── variables.tf                # var.project_id, var.policy_overrides (map)
│   │   └── outputs.tf
│   ├── NetworkModule/
│   │   ├── main.tf                     # VPC + subnet + firewall + Private Google Access
│   │   ├── variables.tf                # var.project_id, var.region, var.network_name
│   │   └── outputs.tf
│   ├── CloudNatModule/
│   │   ├── main.tf                     # Cloud Router + NAT
│   │   ├── variables.tf                # var.project_id, var.region, var.network_name
│   │   └── outputs.tf
│   └── BudgetAlertModule/
│       ├── main.tf                     # Billing budget + alert thresholds
│       ├── variables.tf
│       └── outputs.tf
│
├── researchers/                         # Per-researcher project declarations
│   ├── JohnDoe-LifeSciences-Genomics/
│   │   └── main.tf                     # Composes CommonComponents for John's project
│   ├── PaulUnderwood-LifeSciences-Genomics/
│   │   └── main.tf
│   └── FlameInstitute-CompSci-JaxResearch/
│       └── main.tf
│
├── presets/                             # Workload-specific presets (API lists, roles, policies)
│   ├── nextflow.tfvars                  # APIs + roles for Nextflow/Batch workloads
│   ├── medsiglip-pathology.tfvars       # APIs + roles for Vertex AI Workbench + GPU
│   ├── toxicology.tfvars                # APIs + roles for TxGemma + GROMACS HPC
│   └── README.md                        # "Pick a preset for your researcher's workload"
│
├── templates/
│   └── researcher-template.tf           # Copy-paste starter for new researchers
│
└── background/
    └── plan.md                          # Design rationale
```

Each researcher's `main.tf` would look like:
```hcl
module "apis" {
  source     = "../../CommonComponents/ApiAllowlistModule"
  project_id = var.project_id
  apis       = var.api_list    # from preset
}

module "service_account" {
  source       = "../../CommonComponents/ServiceAccountModule"
  project_id   = var.project_id
  sa_name      = "${var.researcher_name}-pipeline-sa"
  display_name = "${var.researcher_display_name} Pipeline SA"
}

module "iam_roles" {
  source     = "../../CommonComponents/IAMRolesModule"
  project_id = var.project_id
  sa_email   = module.service_account.email
  roles      = var.iam_roles   # from preset
}
# ... network, nat, org-policies, budget
```

---

## Part 3: The Visualization — `fast-science-workload-visualization`

This is the most interesting architectural question. Here's my thinking:

### The Problem
The existing workload-viz repos are **actor + observer** — they both deploy infrastructure AND visualize it. You want a **pure observer** that shows the full L0→L1→L2 estate without deploying anything.

### Architecture: Read-Only State Observer

```
┌─────────────────────────────────────────────────────────────────────────┐
│                fast-science-workload-visualization                       │
│                                                                         │
│  Frontend (React Flow)              Backend (Flask, read-only)          │
│  ┌──────────────────────┐          ┌───────────────────────────────┐   │
│  │                      │          │                               │   │
│  │  ┌─────┐  ┌─────┐   │   /api   │  State Sources:               │   │
│  │  │ L0  │→ │ L1  │   │ ───────> │                               │   │
│  │  │ Hub │  │Spoke│   │          │  1. TF State (GCS backends)   │   │
│  │  └─────┘  └──┬──┘   │          │     gs://tf-state-bucket/     │   │
│  │              │       │          │     ├── l0-bootstrap.tfstate  │   │
│  │         ┌────┴────┐  │          │     ├── l1-john-doe.tfstate   │   │
│  │         │ L2      │  │          │     └── l1-paul-u.tfstate     │   │
│  │         │Workloads│  │          │                               │   │
│  │         │┌──┐┌──┐ │  │          │  2. GCS Status Markers        │   │
│  │         ││NF││Tx│ │  │          │     gs://project-bucket/      │   │
│  │         │└──┘└──┘ │  │          │     └── status/*.json         │   │
│  │         └─────────┘  │          │                               │   │
│  └──────────────────────┘          │  3. GCP Resource APIs         │   │
│                                    │     (read-only: list, get)    │   │
│                                    │     - Compute (VMs, VPCs)     │   │
│                                    │     - IAM (SAs, roles)        │   │
│                                    │     - Vertex AI (endpoints)   │   │
│                                    │     - Service Usage (APIs)    │   │
│                                    │     - Cloud Batch (jobs)      │   │
│                                    │                               │   │
│                                    └───────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### Three State Sources (prioritized):

**1. Terraform State Files (BEST for L0 + L1)**
- Stellar Engine FAST stages store state in GCS backends (e.g., `gs://tf-state-{org}/fast/0-bootstrap.tfstate`)
- L1 researcher projects also store state in GCS
- The visualizer reads these state files and extracts resource inventories
- **This is the gold standard** — if it's in the state file, it exists and is managed

```python
# backend/state_reader.py
class TerraformStateReader:
    """Read Terraform state files from GCS backends."""
    
    def read_state(self, bucket: str, path: str) -> dict:
        """Parse .tfstate JSON and extract resource inventory."""
        blob = gcs_client.bucket(bucket).blob(path)
        state = json.loads(blob.download_as_text())
        return {
            "resources": self._extract_resources(state),
            "outputs": state.get("outputs", {}),
            "serial": state.get("serial"),
        }
    
    def _extract_resources(self, state: dict) -> list:
        """Extract resource type, name, and key attributes."""
        # Returns: [{"type": "google_project", "name": "...", "attrs": {...}}, ...]
```

**2. GCS Status Markers (BEST for L2)**
- Your existing workload-viz pattern writes `status/{step-id}.json` markers to GCS
- The read-only visualizer simply polls these markers
- This is already battle-tested in all three workload-viz repos

**3. GCP Resource APIs (FALLBACK/VALIDATION)**
- For real-time checks when TF state might be stale
- `serviceusage.services.list` — which APIs are enabled?
- `iam.serviceAccounts.list` — does the SA exist?
- `compute.networks.list` — does the VPC exist?
- `aiplatform.endpoints.list` — is TxGemma deployed?
- All read-only, no mutations

### How the Visualization Works

The React Flow canvas would show **three nested zoom levels**:

```
ZOOM LEVEL 1: Organization View
════════════════════════════════
┌─────────────────────────────────────────────────────────────┐
│ L0: University GCP Org (Stellar Engine)                      │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ Stage 0  │→ │ Stage 1  │→ │ Stage 2  │→ Stage 3         │
│  │Bootstrap │  │ Resman   │  │Networking│   Security        │
│  │ ✅ Done  │  │ ✅ Done  │  │ ✅ Done  │   ✅ Done         │
│  └──────────┘  └──────────┘  └──────────┘                   │
│                                                              │
│  Hub Project: org-network-hub                                │
│  └── Shared VPC, Cloud NAT, Firewall policies               │
│                                                              │
│  ┌─── Spoke Projects ──────────────────────────────────────┐ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │ │
│  │  │ John Doe    │  │ Paul U.     │  │ Flame Inst. │     │ │
│  │  │ Genomics    │  │ Genomics    │  │ JAX/ML      │     │ │
│  │  │ ✅ Ready    │  │ ✅ Ready    │  │ 🔄 Building │     │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘

ZOOM LEVEL 2: Click on "John Doe" spoke → L1 Detail
═════════════════════════════════════════════════════
┌──────────────────────────────────────────────────────────────┐
│ L1: John Doe - Dept of Life Sciences - Genomics              │
│ Project: univ-lifesci-johndoe                                │
│                                                              │
│  IT Admin Box (from TF state)                                │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ ✅ APIs (6/6)  ✅ SA Created  ✅ IAM (5 roles)         │ │
│  │ ✅ VPC Ready   ✅ Cloud NAT   ✅ Org Policies          │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  Researcher Box (from GCS markers)                           │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ Blueprint: Nextflow-Batch                                │ │
│  │ ✅ Pipeline deployed  ✅ 3 runs completed  🔄 1 running │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘

ZOOM LEVEL 3: Click on "Researcher Box" → L2 Detail (same as existing workload-viz)
══════════════════════════════════════════════════════════════════
Shows the exact same React Flow from workload-viz-nextflow-batch,
but in READ-ONLY mode, reflecting actual state from GCS markers.
```

### Config-Driven: `estate.yaml`

The visualizer would be configured with a YAML file describing the cloud estate:

```yaml
# estate.yaml — defines what the visualizer observes
organization:
  name: "University Research Cloud"
  
l0_foundation:
  state_bucket: "tf-state-univ"
  stages:
    - name: "0-bootstrap"
      state_path: "fast/0-bootstrap/default.tfstate"
    - name: "1-resman"
      state_path: "fast/1-resman/default.tfstate"
    - name: "2-networking"
      state_path: "fast/2-networking/default.tfstate"
    - name: "3-security"
      state_path: "fast/3-security/default.tfstate"

l1_researchers:
  - name: "John Doe"
    department: "Life Sciences"
    project_id: "univ-lifesci-johndoe"
    state_bucket: "tf-state-univ"
    state_path: "researcher-lab/john-doe/default.tfstate"
    bucket_name: "univ-lifesci-johndoe-bucket"
    blueprint: "nextflow"
    
  - name: "Paul Underwood"
    department: "Life Sciences"
    project_id: "univ-lifesci-paul"
    state_bucket: "tf-state-univ"
    state_path: "researcher-lab/paul/default.tfstate"
    bucket_name: "univ-lifesci-paul-bucket"
    blueprint: "toxicology"

l2_blueprints:
  nextflow:
    status_markers: ["enable-apis", "create-sa", "iam-roles", "create-network", "configure-cloud-nat", "run-pipeline"]
  toxicology:
    status_markers: ["enable-apis", "create-sa", "iam-roles", "create-network", "configure-cloud-nat", "deploy-txgemma", "initialize-rag", "tier1-rag", "tier2-txgemma", "tier3-gromacs"]
  medsiglip:
    status_markers: ["enable-apis", "create-sa", "iam-roles", "create-network", "configure-cloud-nat", "provision-workbench", "load-model", "download-dataset", "fine-tune", "save-results"]
```

### Repo Structure:

```
fast-science-workload-visualization/
├── README.md
├── backend/
│   ├── main.py                    # Flask app, read-only polling
│   ├── state_reader.py            # TF state file parser (GCS)
│   ├── gcs_marker_reader.py       # Status marker aggregator
│   ├── resource_checker.py        # GCP API read-only checks
│   ├── estate_config.py           # Loads estate.yaml
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── OrgView.tsx        # L0 zoom level (org → stages)
│   │   │   ├── ProjectView.tsx    # L1 zoom level (IT box + researcher box)
│   │   │   ├── WorkloadView.tsx   # L2 zoom level (reuses workload-viz components)
│   │   │   ├── SetupStepNode.tsx  # Shared from existing repos
│   │   │   ├── PipelineTaskNode.tsx
│   │   │   └── ...
│   │   └── ...
│   └── ...
├── estate.yaml                    # Configuration file
└── background/
    └── plan.md
```

---

## Summary of Immediate Actions

When you toggle to Act mode, I'll:

1. **Clone** `workload-viz-nextflow-batch` and `workload-viz-medsiglip-pathology` into Projects/
2. **Create** `fast-science-1-researcher-lab/` with:
   - CommonComponents/ (6 TF modules extracted from the workload-viz IT patterns)
   - researchers/ (3 example declarations)
   - presets/ (nextflow, medsiglip, toxicology tfvars)
   - README with step-by-step guide
3. **Create** `fast-science-1-researcher-lab` as a private repo on GitHub

The visualization repo (`fast-science-workload-visualization`) we can start as a design doc in the background/ folder of L1 for now, or create it as a separate repo — your call.

Internal brainstorm site: go/fast-science-repos-drafts